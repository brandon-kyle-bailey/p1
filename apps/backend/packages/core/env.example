# .env (replace values as needed)
PORT=3000

# Redis must point to the service name, not localhost (inside containers)
REDIS_URL=redis://localhost:6379

# DB vars used by your app (we'll use DB_* in your Nest config)
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASS=postgres
DB_NAME=p1

OPENAI_API_KEY=

API_SECRET=4b7ad3ec-a5ab-4f4f-852f-55797db5258a
